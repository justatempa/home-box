# Home Box 配置文件示例
# 复制此文件为 config.env 并修改配置

# ==================== 应用配置 ====================

# 应用名称
APP_NAME="home-box"

# 运行端口
PORT=3002

# 运行环境 (production 或 development)
NODE_ENV=production

# ==================== 数据库配置 ====================

# 数据库连接字符串
# SQLite: file:./prisma/prod.db
# PostgreSQL: postgresql://user:password@localhost:5432/dbname
# MySQL: mysql://user:password@localhost:3306/dbname
DATABASE_URL="file:./prisma/prod.db"

# ==================== 认证配置 ====================

# NextAuth 密钥（生产环境请修改为随机字符串）
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="请修改为随机字符串"

# 应用访问地址（如果使用域名，请修改为实际域名）
# 示例: https://your-domain.com
NEXTAUTH_URL="http://localhost:3002"

# ==================== 管理员账号 ====================

# 管理员用户名
ADMIN_USERNAME="admin"

# 管理员密码（首次登录后请立即修改）
ADMIN_PASSWORD="请修改为强密码"

# ==================== 日志配置 ====================

# 日志文件路径（相对于项目根目录）
LOG_DIR="logs"
LOG_FILE="logs/app.log"
ERROR_LOG_FILE="logs/error.log"

# ==================== 进程管理 ====================

# PID 文件路径
PID_FILE=".home-box.pid"

# ==================== 其他配置 ====================

# 上传文件目录
UPLOAD_DIR="public/uploads"

# 是否启用调试模式 (true/false)
DEBUG=false
